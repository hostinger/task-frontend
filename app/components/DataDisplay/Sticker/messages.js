/*
 * Sticker Messages
 *
 * This contains all the text for the Sticker component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Sticker';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Sticker component!',
  },
});
