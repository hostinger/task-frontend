/*
 * TimerHolder Messages
 *
 * This contains all the text for the TimerHolder component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TimerHolder';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TimerHolder component!',
  },
});
