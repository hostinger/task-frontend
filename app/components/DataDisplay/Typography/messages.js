/*
 * Typography Messages
 *
 * This contains all the text for the Typography component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Typography';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Typography component!',
  },
});
