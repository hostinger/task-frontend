/*
 * ButtonNavigation Messages
 *
 * This contains all the text for the ButtonNavigation component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ButtonNavigation';

export default defineMessages({
  on_sale: {
    id: `${scope}.on_sale`,
    defaultMessage: 'On sale',
  },
});
