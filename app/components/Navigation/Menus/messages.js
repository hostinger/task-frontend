/*
 * Menus Messages
 *
 * This contains all the text for the Menus component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Menus';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Menus component!',
  },
});
