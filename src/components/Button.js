import React from 'react';

const Button = () => {
  return (
    <React.Fragment>
      <button type="button" className="btn">GET STARTED NOW</button>
    </React.Fragment>
  );
};

export default Button;